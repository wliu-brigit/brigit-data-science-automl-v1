# Fraud Control Skeleton Handoff

This file is a compatibility pointer. The current handoff lives in
[`handsoff.md`](handsoff.md).

Keep only one active handoff narrative to avoid stale pickup instructions. The
current state is the forward-only `neo4j_codex` control-loop implementation:
`control_loop_report` is the operator entry point, old representative runner
modules are removed, and generated reports live under
`projects/fraud_anomaly_detection/neo4j_codex/reports/`.
